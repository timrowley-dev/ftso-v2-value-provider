# Log level 
# debug = all detailed logging
# info = important operational messages
# warn = warnings and errors
# error = errors only

LOG_LEVEL=info

# Optional
VALUE_PROVIDER_CLIENT_PORT=3101
VALUE_PROVIDER_CLIENT_BASE_PATH=
# Set to "fixed" to use a fixed provider, leave empty for ccxt provider
VALUE_PROVIDER_IMPL=

# PRICE_SOURCE=ccxt # 'database' or 'ccxt'

# Set to method used. Options: average, weighted-median
PRICE_CALCULATION_METHOD=weighted-median

# Preferred currency pairs. Options: usdt, usdc, both
PREFERRED_CURRENCY_PAIRS=usdt

# Optional - Fine-tune the pricing calculations
MEDIAN_DECAY=0.0001      # Default is 0.00005 if not set

# Outlier detection threshold using Median Absolute Deviation (MAD)
# Values are multipliers of MAD from the median price
# 3-4 = moderate threshold (more aggressive outlier removal)
# 5-6 = conservative threshold (recommended)
# >6 = very conservative (fewer outliers removed)
OUTLIER_THRESHOLD=5.0

PREDICTOR_HOST=127.0.0.1
PREDICTOR_PORT=8000
PREDICTOR_ENABLED=true
PREDICTOR_MODEL=linear
PREDICTOR_LOOKBACK=90
PREDICTOR_CONVERT_TO_USD=true